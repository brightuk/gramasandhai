<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Existing permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />



    <!-- Additional permissions for printing and file operations -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- For Android 10 (API 29) and above -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- For printing to PDF and saving files -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- For network printing (if using cloud print services) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- For camera feature -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.Gramasandhai"

        tools:targetApi="31">



        <!-- FileProvider for file sharing and camera operations -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Splash Activity -->
        <activity
            android:name=".Activity.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Gramasandhai.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".Activity.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.Gramasandhai.NoActionBar" />

        <!-- Main Activity -->
        <activity
            android:name=".Activity.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Gramasandhai.NoActionBar" />

        <!-- Orders Management Activity -->
        <activity
            android:name=".Activity.OrdersManageActivity"
            android:exported="false"
            android:theme="@style/Theme.Gramasandhai.NoActionBar" />

        <!-- Product Management Activities -->
        <activity
            android:name=".Activity.ProductManageActivity"
            android:exported="false"
            android:theme="@style/Theme.Gramasandhai.NoActionBar" />

        <activity
            android:name=".Activity.ProductViewActivity"
            android:exported="false"
            android:theme="@style/Theme.Gramasandhai.NoActionBar" />

        <activity
            android:name=".Activity.AddProductActivity"
            android:exported="false"
            android:theme="@style/Theme.Gramasandhai.NoActionBar" />

        <activity
            android:name=".Activity.AddVariantActivity"
            android:exported="false"
            android:theme="@style/Theme.Gramasandhai.NoActionBar" />

    </application>
</manifest>